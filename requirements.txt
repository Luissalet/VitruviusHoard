# Pinned to the versions verified in the build environment (Python 3.11; 3.11+ required, tested layout for 3.13).
# Every package ships wheels for Windows / Python 3.13. Playwright needs `python -m playwright install chromium`
# once after installing these (or a system Chromium/Edge, see README).
fastapi==0.141.1
uvicorn==0.46.0
pydantic==2.13.3
starlette==1.0.0
httpx==0.28.1
mcp==1.27.0
playwright==1.56.0
pillow==12.2.0
pytest==9.1.1
assay-ui==0.1.4
